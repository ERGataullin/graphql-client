import 'package:flutter/material.dart';

class DirectorsView extends StatelessWidget {
  const DirectorsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
